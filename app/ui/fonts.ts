import { Rubik, Playfair, Playfair_Display } from 'next/font/google'
 
export const rubik = Rubik({
  subsets: ['latin'],
  display: 'swap',
})
 
export const playfair = Playfair({
  subsets: ['latin'],
  display: 'swap',
})
 
export const playfair_display = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
})
